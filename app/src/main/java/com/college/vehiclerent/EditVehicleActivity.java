package com.college.vehiclerent;

// EditVehicleActivity is handled entirely inside AddVehicleActivity.
// When you pass a "vehicleId" extra to AddVehicleActivity it switches
// to edit mode automatically.  This stub exists only so the
// AndroidManifest entry compiles.  You can delete it if unused.

public class EditVehicleActivity extends AddVehicleActivity {
    // Inherits all behaviour from AddVehicleActivity
}
